#pragma once

#include <vector>
#include <string>
#include <cstdint>
#include "../Registers.h"

namespace Backend::CodeGen::Asm::Command
{
    //template <typename Reg>
    //static std::vector<int8_t> xor(Reg reg, std::string& debug)
    //{
        /* [0 1 0 0 W R X B ] */
//#ifdef DEBUG
        //debug = "\nxor " + register_to_string(reg) + ", " + register_to_string(reg);
//#endif
        //std::vector<uint8_t> vec;
        //vec.push_back(0b01001000);
        //vec.push_back(0x31);

        //uint8_t byte = 0x00;
        //byte |= (1 << 7);
        //byte |= (1 << 6);
        //register_to_binary(reg, byte, 5);
        //register_to_binary(reg, byte, 2);
        //vec.push_back(byte);

        //return vec;
    //}
}
